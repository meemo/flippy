# Changelog

## 0.1.0: Initial commit
- CLI for parsing and extractoni
- Metadata output as JSON (pretty-printed by default, `--minified` flag)
- Audio extraction to WAV (PPM IMA ADPCM, KWZ variable-width ADPCM, track mixing)
- Thumbnail and frame extraction to BMP
- Overwrite confirmation with `--force` flag
